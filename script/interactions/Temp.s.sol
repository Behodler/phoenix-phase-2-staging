// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

import "@forge-std/Script.sol";
import "@forge-std/console.sol";

/**
 * @title Temp
 * @notice Scratchpad script for ad-hoc, throwaway mainnet/local interactions.
 * @dev This file is intentionally empty in version control. It is committed once
 *      as a blank skeleton and then ignored locally (git update-index
 *      --skip-worktree), so whatever you scribble here is NOT tracked. A fresh
 *      checkout always gets this empty version. Run via:
 *        npm run Temp:preview     (mainnet fork, deployer sender, no broadcast)
 *        npm run Temp:broadcast   (broadcasts with the deployer Ledger key)
 */
contract Temp is Script {
    function run() external {
        // Scratch work goes here. Nothing written below is committed.
    }
}
