{
  "name": "phstaging2-local-deploy",
  "version": "1.0.0",
  "description": "Local deployment orchestration for Phoenix Phase 2 contracts",
  "scripts": {
    "//local-deploy": "Story 003 — local Anvil deployment orchestration",
    "start:anvil": "anvil --host 0.0.0.0 --port 8545 --chain-id 31337 --block-time 2",
    "clean:local": "rm -rf broadcast/*/31337 server/deployments/progress.31337.json server/deployments/local.json",
    "//deploy-local-forge": "Story 081 — the bare forge leg of deploy:local, split out so dev-local.sh can run clean:local ONCE before anvil starts. Cleaning after anvil is up deletes the broadcast/*/31337 directory the very next forge script writes into. deploy:local keeps its clean+deploy meaning for standalone callers (verify-stable-staker.sh)",
    "deploy:local:forge": "forge script script/DeployMocks.s.sol:DeployMocks --rpc-url http://localhost:8545 --broadcast --slow --gas-estimate-multiplier 300",
    "deploy:local": "npm run clean:local && npm run deploy:local:forge",
    "//extract-addresses": "Story 013 — parameterize extract-addresses for multi-chain",
    "extract:addresses": "node server/extract-addresses.js 31337",
    "//generate-ts": "Story 028 — generate TypeScript address interface (anvil only; mainnet-addresses.ts is hand-maintained, story 068)",
    "generate:ts-anvil": "node server/generate-ts-addresses.js 31337",
    "//serve-and-dev": "Story 003 — API server + local dev orchestration",
    "serve": "node server/index.js",
    "dev": "./dev-local.sh",
    "//wagmi-hooks": "Story 004 — Phase 2 Wagmi hooks package",
    "generate:hooks": "wagmi generate",
    "publish:hooks": "cd hooks && npm publish",
    "version:hooks": "cd hooks && npm version",
    "update:hooks": "npm run generate:hooks && npm run version:hooks patch && npm run publish:hooks",
    "//mint-phusd": "NO_STORY — mainnet operational: mint phUSD",
    "mint-phusd": "bash scripts/mint-phusd.sh",
    "//eclp-pool": "Story 022 — create / verify / initialize Balancer ECLP phUSD/sUSDS pool",
    "mainnet:verify-eclp-fork": "forge test --fork-url $RPC_MAINNET --match-contract VerifyECLPSymmetry -vvv",
    "//phusd-amm-balances": "NO_STORY — mainnet operational check on AMM balances",
    "mainnet:phusd-amm-balances": "bash scripts/check-phusd-amm-balances.sh",
    "//sim-eclp-rebalance": "NO_STORY — fork simulation of ECLP rebalance",
    "sim:eclp-rebalance": "forge test --fork-url $RPC_MAINNET --match-test testSimulateRebalance -vvv",
    "//test-integration-usde": "Story 036 — integration test for ERC4626 market + USDe fork",
    "test:integration-erc4626-market-usde": "forge test --fork-url $RPC_MAINNET --match-contract ERC4626MarketYieldStrategy_USDeFork -vvv",
    "//phlimbo-v1-to-v2": "Story 049 — PhlimboEA V1 → PhlimboV2 silent staker migration (snapshot stakers, then migrate)",
    "snapshot:phlimbo-v1": "node scripts/snapshot-phlimbo-v1-stakers.js",
    "//verify-stable-staker": "Story 051, retargeted onto StableStakerV2 by story 080 — end-to-end config verification (deploy, stake, evm_increaseTime 1 day, assert ~10 Antimatter accrued + claim still closed, withdraw, assert principal/baseline restored and the reward backlog banked)",
    "test:stable-staker": "./verify-stable-staker.sh",
    "//migration-saga": "Story 060+065 migration saga — KEYS BELOW ARE IN ASCENDING EXECUTION ORDER; run top to bottom. For each numbered step: run the -preview key, read the -vvv output, then run the broadcast key (Ledger, hd-path index 46, owner 0xCad1..D0B6). Gather steps are read-only node scripts (no preview). The break-glass keys at the very bottom are an OUT-OF-BAND contingency for a mid-suite halt — NOT part of the linear flow. Prereq satisfied: YS-01 fixed (lib/vault@0110ce4). No --skip-simulation on any step.",
    "//saga-04-gather-leg1": "STEP 4 — snapshot original-staker stakers for leg1 (read-only; re-run if leg1 preflight reports a stale count)",
    "migrate:ys-swap-gather-leg1": "node scripts/gather-migration-inputs.js --leg 1",
    "//saga-07-gather-leg2": "STEP 7 — snapshot temp-staker stakers for leg2 (read-only; run after reset)",
    "migrate:ys-swap-gather-leg2": "node scripts/gather-migration-inputs.js --leg 2",
    "//Temp": "NO_STORY — throwaway scratchpad script (script/interactions/Temp.s.sol). The file is committed empty and then skip-worktree'd locally, so scratch work is never tracked. Preview runs on a mainnet fork as the deployer; broadcast signs with the deployer Ledger key",
    "Temp:preview": "PREVIEW_MODE=true forge script script/interactions/Temp.s.sol:Temp --rpc-url $RPC_MAINNET --sender 0xCad1a7864a108DBFF67F4b8af71fAB0C7A86D0B6 --slow -vvv",
    "Temp:broadcast": "forge script script/interactions/Temp.s.sol:Temp --rpc-url $RPC_MAINNET --sender 0xCad1a7864a108DBFF67F4b8af71fAB0C7A86D0B6 --broadcast --skip-simulation --slow --ledger --hd-paths \"m/44'/60'/46'/0/0\" -vvv",
    "//promotion-ready": "Story 072 - THE PROMOTION-READY CUTOVER. One differential mainnet broadcast promoting the accumulated nft-staking (9611312) / yield-claim-nft (9c18020) / stable-yield-accumulator (6eab35c) work. Five coupled changes: (1) deploy NudgeStreamer and convert every donor from 'push USDC at the batch-minter' to 'approve + collectNudge through the streamer', which buffers each donation and releases it linearly instead of dumping it in a burst the next batchMint caller captures whole; (2) redeploy all four donor dispatchers - BalancerPoolerV2 (idx 4, SIX-arg ctor, prime token DERIVED from sUSDS), Uniboost x3 (idx 1/2/3) and NudgeRatchet (idx 7, replacing the NudgeRatchetDelayRelease stopgap) - plus StableYieldAccumulator (NO-ARG ctor: the broadcasting Ledger key becomes owner); every live instance is a pre-streamer build, verified by nudgeStreamer() reverting on all of them; (3) replace the shared BatchNFTMinter with BatchNFTMinterMultiToken, whitelist USDC/phUSD/Kendu and registerStream at 10/30/30 days, routing the rescued pot THROUGH the streamer rather than onto the minter; (4) silently migrate the three NFTStakerDepletion instances to NFTStakerDepletionV2 via NFTStakerMigrator - zero user action; (5) retire the old batch minter (setPauser(OWNER) + pause(), deliberately NOT registered with the global Pauser). THE FIVE MINT-DEBT HOOKS ARE REPOINTED, NOT REDEPLOYED (dispatcher is mutable storage with an owner-only setter), so there are ZERO phUSD.setMinter calls in the whole script and each hook keeps its ratio/recipient/mintDebt. Per index the order is pull() -> hook.setDispatcher(new) -> new.setHook(hook) -> replaceDispatcher(idx, new): fail-closed. THE REVERSE ORDER LEAKS VALUE SILENTLY (the new dispatcher would go live carrying the fresh DefaultDispatchHook and accrue no mint debt). Expect user-visible mint failures on indices 1/2/3/4/7 for the duration of the Ledger session - that is the correct failure direction. PREREQUISITE: run promotion-ready:snapshot first; Phase 6 reads its migrate() user lists from scripts/snapshots/depletion-stakers-latest.json. KNOWN BREAKAGE, KNOWINGLY ACCEPTED: whitelisting three reward tokens makes batchMint's minRewards array length-3, so the live mint UI is BROKEN until a phlimbo-ui story ships a 3-element array plus a getNudgeTokens() re-fetch before each batch (the order changes on removal - swap-and-pop). Two batch-minter ABIs now coexist: 4-arg multi-token on the shared minter, legacy on the other four. MANUAL TOP-UPS: phUSD and Kendu have no donor contract; the owner funds them with IERC20(token).forceApprove(streamer, amount) then streamer.collectNudge(newBatchMinter, token, amount). Owner 0xCad1a7864a108DBFF67F4b8af71fAB0C7A86D0B6, Ledger HD m/44'/60'/46'/0/0. ================ STORY 076 -- PHASE 4e, THE PHLIMBO V3 CUTOVER ================ Story 072 was planned before the PhlimboV3 promo work landed and never contemplated it: its Phase 5 deployed a fresh StableYieldAccumulator and pointed it straight back at the OLD Phlimbo (sya.setPhlimbo(old.phlimbo()) -> PHLIMBO_V2), so the whole 'promotion-ready' stack ended up wired to a Phlimbo with NO promotional-reward machinery at all -- the very feature the sprint is named for. Phase 4e closes that, sequenced IMMEDIATELY BEFORE Phase 5 so Phase 5 can name PhlimboV3 directly instead of mirroring V2 and correcting afterwards. It: deploys PhlimboV3 (phUSD + rewardToken + depletionDuration all read LIVE off V2); commits its APY via the TWO-STEP preview->commit setter (a single call silently does NOTHING -- PhlimboV3.sol:261-280); registers it with the global Pauser; GRANTS IT phUSD MINT AUTHORITY; deploys MigratorV2V3 and sets the migrator role on BOTH sides (a half-met pair silently skips every user); seeds from the V2 snapshot and walks migrate() in chunks, reseeding for stragglers; then winds V2 down and revokes its mint authority. THIS BREAKS TWO OF STORY 072's STATED INVARIANTS, DELIBERATELY. 'ZERO phUSD.setMinter calls in this entire script' is now false -- there are exactly two, +PhlimboV3 and -PhlimboV2 -- and the 'minter set byte-identical' post-broadcast claim is replaced by an ASSERTED TWO-SIDED DELTA in promotion-ready:verify: the candidate mask must have lost exactly the PhlimboV2 bit and nothing else, and PhlimboV3 must POSITIVELY hold mint authority. THE MINT GRANT MUST BE ASSERTED BECAUSE ITS ABSENCE IS SILENT: PhlimboV3 pays the phUSD leg with try phUSD.mint(...) {} catch { bank } (PhlimboV3.sol:913, audit-09 M-01), so a forgotten grant reverts NOTHING -- every staker just accrues an unpayable entitlement while the stable leg keeps paying, surfacing only as user complaints. PHLIMBO V2 IS WOUND DOWN, NEVER PAUSED -- THE EXACT INVERSE OF PHASE 6. PhlimboV2.withdraw is whenNotPaused and the migrator calls exactly that, so pausing V2 blocks the migrator; worse, the per-user body runs inside a try/catch, so a paused V2 yields a pass that COMPLETES with every user skipped and looks identical to a good one (MigratorV2V3.sol:22-24, :65-73). The wind-down is operational: setDesiredAPY(0) x2, setMigrator(0), and Phase 5's accumulator repoint (which is what actually stops the feed). The phUSD mint revoke on V2 comes AFTER the APY zeroing and AFTER the totalStaked()==0 gate, never before either: PhlimboV2._claimRewards mints with a BARE, REVERTING phUSD.mint (PhlimboV2.sol:495), so revoking while a position still carried pending phUSD would freeze that user's principal. With totalStaked==0 no position can reach the mint, and with APY 0 a late V2 staker accrues zero pending phUSD and still exits cleanly. STRAGGLERS ARE A FAILURE CONDITION, NOT AN ACCEPTED RESIDUE (owner decision 2026-08-03): Phase 4e require()s phlimboV2.totalStaked()==0 and prints every decoded UserMigrationSkipped reason first (empty reason = sub-MINIMUM_STAKE dust, non-empty = revert data, usually misconfiguration). If the gate cannot be satisfied, STOP AND REPORT -- do not relax it. NO PROMOTION IS ARMED: promoToken==address(0) is a first-class designed-for state at all nine of its consumption sites, startPromotion is a separate later owner action with its own funding and depletion clock, and Phase 7 asserts the negative. Kendu is deliberately NOT set as a promo token. PhlimboV3 gets a mainnet-addresses.ts key; MigratorV2V3 is transient and gets none. STORY 076 ADDED A SECOND SNAPSHOT to this key, joined with '&&' so a failure in either halts the chain: scripts/snapshot-phlimbo-v2-stakers.js writes scripts/snapshots/phlimbo-v2-snapshot-latest.json, which Phase 4e seeds MigratorV2V3 from. Unlike the depletion-staker snapshot it is an ADDRESS LIST ONLY -- MigratorV2V3 reads every position live off V2 (MigratorV2V3.sol:20-24), so no amount is seeded and none could be trusted if it were. It must NOT be taken after a pause: PhlimboV2 is deliberately never paused for this migration.",
    "promotion-ready:snapshot": "RPC_MAINNET=$RPC_MAINNET node scripts/snapshot-depletion-stakers.js && RPC_MAINNET=$RPC_MAINNET node scripts/snapshot-phlimbo-v2-stakers.js",
    "//order-of-execution:human-readable": "promotion-ready:snapshot then  promotion-ready:dry then promotion-ready:broadcast. After success, mint an LSP NFT and check that it truly pools on balancer. If the cutover fails, consult Claude on how to resume."
  },
  "keywords": [
    "phoenix",
    "deployment",
    "foundry",
    "local",
    "development"
  ],
  "author": "Behodler",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@wagmi/cli": "^2.8.0"
  }
}
