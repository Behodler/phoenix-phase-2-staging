cache/
node_modules

out/
.npmrc
.envrc

broadcast/Temp.s.sol/
# Uncommitted local docs (not tracked)
docs/uncommitted/

# Story 072: chain-state snapshots for the promotion-ready cutover.
# Regenerated by `npm run promotion-ready:snapshot` immediately before each broadcast;
# stale user lists must never be committed or silently reused.
scripts/snapshots/
